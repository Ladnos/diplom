{
  "name": "@crm/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "gRPC-контракты, типы событий и топология RabbitMQ — общие для всех сервисов",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "proto"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.7.13"
  }
}
